fix(ph): replace the PSE widgets that never rendered, and give the market real panels

Reported from a screenshot, and both PSE TradingView panels were blank for reasons no
configuration would have fixed:

- **The index chart** answered "This symbol is only available on TradingView" - an embed
  entitlement on PSE:PSEI, not a config mistake.
- **The quotes board** came back empty for PSE:-prefixed symbols, so the entitlement extends to
  the exchange's equities.

They are replaced rather than repaired, with panels built from the feed that does work:

- **PSE movers** - advancers against decliners, the day's average move, and the extremes. Counted
  over *traded* names only: the feed lists 385 symbols including untraded ones, and folding their
  flat zeroes into the average would report a calmer market than the one that traded.
- **PH business news** - TradingView's timeline widget has no PSE market (its `market` parameter
  offers stock, crypto and forex), so this cannot be a widget swap either. It reads the
  publishers' own feeds, and only the three checked by hand are listed: BusinessWorld redirects,
  GMA's business feed 404s, and Manila Bulletin returns 403 to a non-browser agent.

**The table no longer grows the page.** Its row was `shrink-0` and sized to content, which is what
pushed the dashboard past the fold; both PSE rows now share the viewport and the table scrolls
inside its own panel, like every other panel in the app.

`parseRssFeed` moved out of crypto.actions.ts into lib/rss.ts, parameterised by category, so the
two markets share one implementation instead of a second copy. The two PSE widget configs are
deleted - nothing rendered them.

Verified: tsc clean, 42 test files, 615 tests passing, build exit 0.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
